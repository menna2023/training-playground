pipeline { 
         agent any
        stages{
         stage ('build') {
            steps {
                   sh 'make Makefile.make'
                   archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
                   }
                   }
 }
 }        
