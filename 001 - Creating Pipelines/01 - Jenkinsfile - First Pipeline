pipeline {
    agent { 
		docker { 
			image 'centos' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh 'cat /etc/alpine-release'
                #sh 'sleep 5d'
            }
        }
    }
}
